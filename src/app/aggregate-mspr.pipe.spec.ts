import { AggregateMSPRPipe } from './aggregate-mspr.pipe';

describe('AggregateMSPRPipe', () => {
  it('create an instance', () => {
    const pipe = new AggregateMSPRPipe();
    expect(pipe).toBeTruthy();
  });
});
